# Виртуальное окружение
.venv/

# Секреты / локальная конфигурация (шаблон .env.example остаётся в репозитории)
.env

# Локальные настройки Claude Code (машинно-специфичные пути)
.claude/

# Python
__pycache__/
*.py[cod]

# pytest
.pytest_cache/

# Генерируемые артефакты оценки детекторов (перегенерируются скриптами;
# examples.json — исходный корпус, он ОСТАЁТСЯ в репозитории)
scripts_for_testing/results/
*.log

LICENSE